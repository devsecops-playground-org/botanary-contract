# Copy to .env and fill in. Never commit real values.
#
# A funded private key belongs in a hardware wallet or the secrets manager,
# never in this repository. The pre-commit hook and GitHub push protection
# both scan for keys that look real.

SEPOLIA_RPC_URL=
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=
REPORT_GAS=false
